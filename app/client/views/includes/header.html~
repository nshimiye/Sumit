<template name="header">
<div class="navbar navbar-default navbar-fixed-top" role="navigation" style="min-width: 1190px;">
    <div class="container"
         style="
         padding-bottom: 0.5em;
         padding-top: 1em;
         min-width: 95%;
         "
         >
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{pathFor 'home'}}" style="padding-top: 0px;">
            
			<img src="/img/logo.png" alt="SUMIT logo" style="height: 35px; vertical-align: text-bottom;">
                    
                    
                     Global Challenges Forum</a>
        </div>
        <div class="navbar-collapse collapse">



            <!--user information here lol!-->
            <ul class="nav navbar-nav navbar-right" style="margin-top: 8px;">

                {{> loginButtons}}

            </ul>

            <!--end of user-->
				{{> searchAutoComplete}}
   


            {{#if currentUser}}
            <!-- to be modified though -->
            <form class="navbar-form navbar-right {{activeRouteClass 'postSubmit'}}">
                <!--a href="{{pathFor 'postSubmit'}}" class="btn btn-primary ">New Post</a-->
                 <a href="#" class="btn btn-primary new_post {{#if show_submit}}active{{/if}}"
                 
                                  style="height: 35px;
padding-top: 0.4em;"
                 
                 >New Post</a>
            </form>
            {{/if}}
        </div><!--/.navbar-collapse -->
    </div>
</div>

<div style="min-height: 7em;">
    <!--navigation bar spacer-->
</div>        
</template>
