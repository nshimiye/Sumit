<template name="header_alt">
<div class="navbar navbar-default navbar-fixed-top" role="navigation" style="min-width: 1190px;">
    <div class="container"
         style="
         padding-bottom: 0.5em;
         padding-top: 1em;
         min-width: 95%;
         "
         >
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{pathFor 'home'}}"><span
                    style="
                    font-size: 2em;
                    font-weight: bold;
                    "
                    >SUMIT</span> - Global Challenges Forum</a>
        </div>
        <div class="navbar-collapse collapse">



            <!--user information here lol!-->





            <ul class="nav navbar-nav navbar-right" style="margin-top: 8px;">

                {{> loginButtons}}

            </ul>

            {{#if currentUser}}
            <ul class="nav navbar-nav navbar-right" style="margin-top: 8px;">
                <li>{{> notifications}}</li>


            </ul>
            {{/if}}
            <!--end of user-->

            <form class="navbar-form navbar-right" role="form" style="position: relative;">

                <div class="form-group navbar-right">
                    <input type="text" class="form-control" 
                    
                    	placeholder="Sarch for problems, challenges, locations" style="width: 500px;">
                </div>
    <div id="autocmp1" class="autocmp">
		{{> searchAutoComplete}}
   
                        </div>
            </form>

            {{#if currentUser}}
            <!-- to be modified though -->
            <form class="navbar-form navbar-right {{activeRouteClass 'postSubmit'}}">
                <a href="{{pathFor 'postSubmit'}}" class="btn btn-primary ">New Post</a>
            </form>
            {{/if}}

        </div><!--/.navbar-collapse -->
    </div>
</div>

<div style="min-height: 7em;">
    <!--navigation bar spacer-->
</div>        
</template>
